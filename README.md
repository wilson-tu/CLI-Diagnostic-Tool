# CLI-Diagnostic-Tool
CLI tool that automates basic network troubleshooting (DNS lookup, ping, traceroute, port scan) to speed up diagnostics.

To use, simply input the ip address or DNS of what you're trying to connect to.
